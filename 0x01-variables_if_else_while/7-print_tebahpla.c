#include <stdio.h>
#include <string.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int X;

	for (X = 122; X > 96; X--)
	{
		putchar(X);
	}
	putchar('\n');
	return (0);
}
